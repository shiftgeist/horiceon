{{- $name := promptString "git name" -}}
{{- $email := promptString "git email" -}}

[data]
  [data.git]
    name = "{{ $name }}"
    email = "{{ $email }}"
  [data.api]
    tinypng = "{{ (bitwarden "item" "tinypng").login.password }}"
