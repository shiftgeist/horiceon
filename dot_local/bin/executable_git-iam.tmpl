#!/bin/sh

echo "GIT: Name? "
read -r git_name
git config user.name "$git_name"
echo ""

echo "GIT: E-Mail? "
read -r git_email
git config user.email "$git_email"
echo ""

git config user.signingkey '{{ .git.gpg }}'
