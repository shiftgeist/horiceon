#!/bin/sh

set -e

main() {
    cli_name=${0##*/}

    cli_help() {
        echo "
Horiceon helper cli
Usage: $cli_name [command]

Commands:
  install|i     install packages

Hint: No/empty command shows help."
        exit 1
    }

    case "$1" in
    install | i)
        install "$@"
        ;;
    docs | d)
        docs "$@"
        ;;
    *)
        cli_help
        ;;
    esac
}

install() {
    cli_help_packages() {
        echo "
Install packages
Usage: $cli_name packages [tool]

Commands:
  arch              arch packages
  code              vscode extensions
  code-optional     optional vscode extensions
  code-languages    language specific vscode extensions
  deb               debian packages
  fzf               install fzf
  go                go packages
  golang            install go [version]
  npm               npm global packages
  volta             node version manager

Hint: No/empty command shows help."
        exit 1
    }

    case "$2" in
    arch)
        if hash yay 2>/dev/null; then
            yay -Syu suckless-tools brave-bin notion-app alacritty zsh neofetch htop neovim code rsync rclone openssh go ttf-dejavu noto-fonts-emoji otf-overpass feh flameshot gpick xsel xcape compton bat exa tmux screen ghostwriter rofi
        else
            echo "Dependency \"yay\" not found."
            exit 1
        fi
        ;;
    code)
        code --install-extension "bierner.emojisense"
        code --install-extension "bierner.markdown-preview-github-styles"
        code --install-extension "dbaeumer.vscode-eslint"
        code --install-extension "eamodio.gitlens"
        code --install-extension "editorconfig.editorconfig"
        code --install-extension "lokalise.i18n-ally"
        code --install-extension "naumovs.color-highlight"
        code --install-extension "ow.vscode-subword-navigation"
        code --install-extension "pflannery.vscode-versionlens"
        code --install-extension "simonsiefke.prettier-vscode"
        code --install-extension "simonsiefke.svg-preview"
        code --install-extension "tinkertrain.theme-panda"
        code --install-extension "waderyan.gitblame"
        code --install-extension "wix.vscode-import-cost"
        code --install-extension "wmaurer.change-case"
        code --install-extension "zengxingxin.sort-js-object-keys"
        ;;
    code-optional)
        code --install-extension "ryu1kn.text-marker"
        ;;
    code-languages)
        # css
        code --install-extension "stylelint.vscode-stylelint" # :warning: slow!

        # svelte
        code --install-extension "fivethree.vscode-svelte-snippets"
        code --install-extension "svelte.svelte-vscode"

        # vue
        code --install-extension "octref.vetur"

        # tailwind
        code --install-extension "heybourn.headwind"
        code --install-extension "bradlc.vscode-tailwindcss"
        code --install-extension "voorjaar.windicss-intellisense" # windicss

        # shell
        code --install-extension "timonwong.shellcheck"
        code --install-extension "foxundermoon.shell-format"

        # yaml
        code --install-extension "redhat.vscode-yaml"

        # mdx
        code --install-extension "silvenon.mdx"
        ;;
    deb)
        sudo apt-get install tmux flameshot ghostwriter zsh telnet git vim htop nload dstat net-tools rofi
        # alacritty
        ;;
    fzf)
        chmod +x ~/.local/src/fzf/install
        ~/.local/src/fzf/install --bin
        ;;
    go)
        go get -u github.com/cosmtrek/air github.com/nektos/act github.com/xxxserxxx/gotop/cmd/gotop github.com/yudai/gotty
        ;;
    golang)
        sudo rm -rf /usr/local/go
        if [ "$3" ]; then
            GO_VERSION="go$3"
        else
            GO_VERSION=$(curl -s "https://golang.org/VERSION?m=text")
        fi
        curl --progress-bar "https://dl.google.com/go/$GO_VERSION.linux-amd64.tar.gz" | sudo tar zxf - -C /usr/local
        go version
        ;;
    npm)
        npm i -g pnpm np doctoc prettier js-beautify tinypng-cli diff-so-fancy npm-check-updates sirv-cli live-server firebase-tools markdownlint-cli semver bundle-phobia-cli imagemin imagemin-webp tldr
        ;;
    volta)
        curl https://get.volta.sh | bash -s -- "--skip-setup"
        ;;
    *)
        cli_help_packages
        ;;
    esac
}

docs() {
    cli_name=${0##*/}

    cli_help_docs() {
        echo "
Usage: $cli_name docs git|posix|shortcuts|units|vim"
        exit 1
    }

    BASE_CHEZMOI="$(chezmoi source-path)/docs/"
    case "$2" in
    git | posix | shortcuts | units | vim)
        less "$BASE_CHEZMOI/$2.md"
        ;;
    *)
        cli_help_docs
        ;;
    esac
}

main "$@"
